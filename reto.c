#include <stdio.h>
#include <string.h>
int main(){
    int salto;
    unsigned char caso [4]={3,5,2,6}; 
    for (int i=0; i < 4; i++){
        for (salto = caso [i]; salto < 4; salto++){
            // se que salto es el valor de cada posicion , pero aun no se como hacer que se recorra el numero de veces que diga la posicion de i
            if (salto )
        }

        // if (salto = caso[i]){
        //     printf("No tiene solucion\n\n");
        // }
        // else{
        //     printf("Si tiene solucion\n\n");
        // }
    }
   return 0;
}